import { RegisterForm } from "@/components/MainComponent/FormRegister"

export default function RegisterPage() {
  return (
    <section className="flex flex-col items-center h-screen p-3">
      <RegisterForm />
    </section>
  )
}
import { LoginForm } from "@/components/MainComponent/FormLogin"
export default function FormLogin() {
  return (
    <section className="flex flex-col items-center justify-center h-screen p-3">
        <h1>LOGIN</h1>
        <LoginForm />
    </section>
  )
}